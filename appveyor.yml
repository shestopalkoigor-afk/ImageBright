image: Visual Studio 2022
stack: python 3

install:
  - python -m pip install --upgrade pip
  - pip install -r requirements.txt

build: off  # Для Python-проектов сборка не требуется

test_script:
  - python -c "import app; print('App imported successfully')"
